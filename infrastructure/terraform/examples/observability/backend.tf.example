# Uncomment and configure for remote state storage.
# Choose the backend matching the cloud where your Kubernetes cluster runs.
#
# AWS (S3):
# terraform {
#   backend "s3" {
#     bucket         = "my-honua-terraform-state"
#     key            = "honua/observability/terraform.tfstate"
#     region         = "us-east-1"
#     encrypt        = true
#     dynamodb_table = "terraform-locks"
#   }
# }
#
# Azure (azurerm):
# terraform {
#   backend "azurerm" {
#     resource_group_name  = "my-honua-tfstate-rg"
#     storage_account_name = "myhounatfstate"
#     container_name       = "tfstate"
#     key                  = "honua/observability/terraform.tfstate"
#   }
# }
